{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "//test": "Needs the blog built (dist/blog/ap/manifest.json) — see federation/smoke.mjs.",
    "test": "node --disable-warning=ExperimentalWarning federation/smoke.mjs"
  },
  "dependencies": {
    "@fedify/fedify": "^2.3.6",
    "@js-temporal/polyfill": "^0.5.1",
    "xss": "^1.0.15"
  },
  "devDependencies": {
    "wrangler": "^4.93.1"
  }
}
