{
  "name": "atfedi-de",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["catalog", "shared", "worker"],
  "scripts": {
    "dev": "npm run dev --workspace catalog",
    "build": "npm run build --workspace catalog",
    "check": "npm run check --workspace catalog",
    "deploy": "npm run build && npm run deploy --workspace worker"
  }
}
