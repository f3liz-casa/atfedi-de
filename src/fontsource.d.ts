// Fontsource packages are CSS-only and ship no type declarations.
// These side-effect imports are valid; this just quiets the type checker.
declare module '@fontsource-variable/*';
declare module '@fontsource/*';
